# ============================================
#  Nana AI VTuber — dependensi Python
# ============================================
# Cara install manual (kalau tidak pakai run.sh):
#   python3 -m venv .venv
#   ./.venv/bin/pip install -r requirements.txt
#
# Dipakai oleh:
#   - app.py  (server web + chat + suara Edge TTS)
#   - main.py (versi terminal dengan suara pyttsx3)

Flask==3.1.3
requests==2.34.2
edge-tts==7.2.8
pyttsx3==2.90
